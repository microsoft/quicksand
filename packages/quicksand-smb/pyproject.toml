[project]
name = "quicksand-smb"
version = "0.4.7"
description = "Pure-Python SMB3 server for quicksand host-guest directory mounts."
requires-python = ">=3.11"
license = "MIT"
dependencies = []

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["quicksand_smb"]
