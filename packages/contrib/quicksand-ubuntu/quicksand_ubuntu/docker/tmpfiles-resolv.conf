# Symlink resolv.conf at boot (Docker can't write to /etc/resolv.conf during build)
# L+ removes existing file/symlink and creates a new symlink
L+ /etc/resolv.conf - - - - /etc/resolv.conf.quicksand
